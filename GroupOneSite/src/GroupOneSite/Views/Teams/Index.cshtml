@model IEnumerable<GroupOneSite.Models.Team>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.memberDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.pictureLink)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.resumeLink)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.memberDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pictureLink)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.resumeLink)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
